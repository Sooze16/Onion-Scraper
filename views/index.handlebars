<div class="row">
    <div class="col-md-12 text-center welcome">

        {{#each articles }}
    <a href="https://www.popularmechanics.com/{{link}}"><h2 id="welcome_title">{{title}}</h2></a>    
    <br>
    {{summary}}
           
        <button type="submit" id="saved" data-id="{{_id}}" class="btn btn-danger btn-sm pull right">Save Article  </button>
   
        {{/each}}
    </div>
</div>